ribbon:
  eureka:
    enabled: true
server:
  port: 8081
spring:
  application:
    name: User-service
  cloud:
    config:
      uri: http://localhost:8888
  h2:
    console:
      enabled: true
      path: /h2
      
  boot:
    admin:
      client:
        url:
        - http://localhost:1111